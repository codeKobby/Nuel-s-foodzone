
// This file can be removed as its content is now part of the root layout.
// For now, just a simple pass-through to avoid breaking Next.js file-based routing.
export default function MainLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
